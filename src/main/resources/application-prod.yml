spring:
  config:
    import: configserver:http://localhost:8888

  cloud:
    config:
      enabled: false
      username: ADMIN
      password: 123
      label: main
      profiles: prod

  application:
    name: "ms-api-boilerplate"