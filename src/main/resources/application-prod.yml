spring:

  config:
    import: configserver:http://configserver-svc:8888

  cloud:
    config:
      enabled: true
      username: ADMIN
      password: 123
      label: main
      profiles: prod

  application:
    name: "ms-api-boilerplate"